version: "3"

services:
  mongodb:
    image: mongo:latest
    read_only: true
    tmpfs:
      - /data/db
      - /data/configdb
      - /tmp
    environment:
      - MONGODB_DATABASE="test"
    ports:
      - 27017:27017